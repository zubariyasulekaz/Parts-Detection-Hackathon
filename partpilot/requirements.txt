# --- web framework ---------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- validation / config ---------------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- database ---------------------------------------------------------------
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# sync driver used by Alembic's migration runner only (the app itself uses asyncpg)
psycopg2-binary==2.9.10

# --- similarity search ---------------------------------------------------------------
# torch is imported directly (embedding_backends.py), not just pulled in
# transitively by transformers, so it is pinned explicitly rather than left to
# whatever version that happens to resolve.
#
# Install the CPU build first on a server - the default PyPI wheel bundles CUDA
# and is 3-4x larger for no benefit on a box without a GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
faiss-cpu==1.9.0.post1
numpy==2.0.2
# Provides DINOv2, including the fine-tuned checkpoint the shipped index was
# built with, loaded through AutoModel from a local directory.
transformers==4.46.3
accelerate==1.1.1

# --- image processing ---------------------------------------------------------------
Pillow==11.0.0
rembg==2.0.60
# rembg runs its segmentation model through ONNX but does not pull the runtime
# in itself; without this it fails at import with a misleading "rembg is not
# installed" error.
onnxruntime==1.20.1

# --- testing ---------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1
