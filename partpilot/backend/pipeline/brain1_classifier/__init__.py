"""Brain 1: Image Classification.

Given a part image, predicts the product category (e.g. "oil_filter",
"brake_pad") using an EfficientNet-based model. See `interfaces.py` for
the contract other pipeline stages depend on.
"""
